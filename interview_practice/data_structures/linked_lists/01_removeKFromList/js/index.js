// Definition for singly-linked list:
// function ListNode(x) {
//   this.value = x;
//   this.next = null;
// }
//

const removeKFromList = (l, k) => {
  while (l)
  return true;
};

const listNode = x => {
  this.value = x;
  this.next = null;
}

module.exports = { fns: [ removeKFromList ] };
